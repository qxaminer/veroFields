// parametrize custom function by passing in custom values
//variables declared in the head of the function are local and hidden from the global
//must declare type
void myRect(float x,float y, float w,float h){
rect(x,y,w,h);
}
